{
  "name": "virus",
  "version": "0.1.0",
  "description": "$VIRUS — This Will Spread. Auto-claims pump.fun creator fees every 5 min, snapshots holders (the infected), spins a holder-weighted wheel, and pays 50% of the claim in SOL to the winner via 2 ephemeral hop wallets. The other 50% goes to a marketing wallet.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.98.0",
    "bs58": "^6.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
